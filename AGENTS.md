# Workspace Instructions

## GitHub Account Policy
- Use GitHub account/owner `joriszwanenburg95-maker` for all repository actions in this workspace.
- Do not use the `Leanfix` GitHub account, token, org context, or the `github-leanfix` SSH host for this project.
- Prefer SSH remotes in this format: `git@github.com:joriszwanenburg95-maker/<repo>.git`.
- Before any `push`, `pull`, `clone`, `repo create`, or PR action, verify that the remote or target repo points to `joriszwanenburg95-maker`.
- If `gh auth status` shows another active account, switch or re-authenticate `gh` to `joriszwanenburg95-maker` before continuing.

## Git Identity
- Use `Joris Zwanenburg <joriszwanenburg95@gmail.com>` as the default Git author identity for this workspace unless the user explicitly asks for a different commit author.

## Repo Setup Note
- This directory may not always be initialized as a Git repository yet. If Git is initialized later, set `origin` to the `joriszwanenburg95-maker` repository before the first push.
